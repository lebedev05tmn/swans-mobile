import { StyleSheet, Text, View } from 'react-native';

const swans = () => {
    return (
        <View>
            <Text>swans</Text>
        </View>
    );
};

export default swans;

const styles = StyleSheet.create({});

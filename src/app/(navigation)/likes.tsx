import { StyleSheet, Text, View } from 'react-native';

const likes = () => {
    return (
        <View>
            <Text>likes</Text>
        </View>
    );
};

export default likes;

const styles = StyleSheet.create({});

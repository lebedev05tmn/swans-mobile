import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    text: {
        color: '#404040',
        fontWeight: 500,
        fontSize: 16,
        lineHeight: 16,
    },
    button: {
        marginTop: 'auto',
        marginBottom: 32,
    }
});

export default styles;

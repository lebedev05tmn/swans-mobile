import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    submitButton: {
        marginBottom: 50,
        marginTop: 'auto',
    },
    text: {
        color: '#404040',
        fontWeight: 500,
        fontSize: 16,
        lineHeight: 16,
    },
});

export default styles;